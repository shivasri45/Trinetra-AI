[pytest]
testpaths = tests
addopts = -q --strict-markers -p no:pytest_ethereum
markers =
    slow: end-to-end runs that drive the full pipeline for hundreds of samples
filterwarnings =
    ignore::DeprecationWarning
    ignore:.*sklearn.utils.parallel.delayed.*:UserWarning
